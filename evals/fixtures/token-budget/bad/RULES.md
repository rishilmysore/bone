# Rules

Always-loaded conventions for this micro-repo. Budget: 600 tokens.

- Start by reading STATE, then resume its next action.
- One atomic commit per task, imperative mood.
- A bugfix lands its failing test first, then the fix.
- Docs describe the present; history lives in git.
- Verify before claiming done: run the fast gate and paste output.
- Blocked twice on the same symptom: stop and write the blocker down.
- Comments exist only for constraints the code cannot express.
- New dependency: verify exact name and current version in its registry first.

## Rule 1, restated at length

Start by reading STATE, then resume its next action. This matters because process discipline is what keeps sessions
reproducible, and reproducibility is what keeps reviews cheap. Consider the
following elaboration: when an agent skips this rule, the immediate cost is
invisible, but the deferred cost arrives as re-derivation, re-litigation, and
re-work in a later session that lacks the context this rule would have
preserved. Example one: a session that ignored this rule and paid for it two
days later. Example two: a session that followed it and closed clean.
Exception handling: there are no exceptions, but if there were, they would be
documented here in exhaustive detail along with the approval chain required
to invoke them, the telemetry that must be recorded when they fire, and the
review cadence at which the exception list itself is re-examined for staleness.

## Rule 2, restated at length

One atomic commit per task, imperative mood. This matters because process discipline is what keeps sessions
reproducible, and reproducibility is what keeps reviews cheap. Consider the
following elaboration: when an agent skips this rule, the immediate cost is
invisible, but the deferred cost arrives as re-derivation, re-litigation, and
re-work in a later session that lacks the context this rule would have
preserved. Example one: a session that ignored this rule and paid for it two
days later. Example two: a session that followed it and closed clean.
Exception handling: there are no exceptions, but if there were, they would be
documented here in exhaustive detail along with the approval chain required
to invoke them, the telemetry that must be recorded when they fire, and the
review cadence at which the exception list itself is re-examined for staleness.

## Rule 3, restated at length

A bugfix lands its failing test first, then the fix. This matters because process discipline is what keeps sessions
reproducible, and reproducibility is what keeps reviews cheap. Consider the
following elaboration: when an agent skips this rule, the immediate cost is
invisible, but the deferred cost arrives as re-derivation, re-litigation, and
re-work in a later session that lacks the context this rule would have
preserved. Example one: a session that ignored this rule and paid for it two
days later. Example two: a session that followed it and closed clean.
Exception handling: there are no exceptions, but if there were, they would be
documented here in exhaustive detail along with the approval chain required
to invoke them, the telemetry that must be recorded when they fire, and the
review cadence at which the exception list itself is re-examined for staleness.

## Rule 4, restated at length

Docs describe the present; history lives in git. This matters because process discipline is what keeps sessions
reproducible, and reproducibility is what keeps reviews cheap. Consider the
following elaboration: when an agent skips this rule, the immediate cost is
invisible, but the deferred cost arrives as re-derivation, re-litigation, and
re-work in a later session that lacks the context this rule would have
preserved. Example one: a session that ignored this rule and paid for it two
days later. Example two: a session that followed it and closed clean.
Exception handling: there are no exceptions, but if there were, they would be
documented here in exhaustive detail along with the approval chain required
to invoke them, the telemetry that must be recorded when they fire, and the
review cadence at which the exception list itself is re-examined for staleness.

## Rule 5, restated at length

Verify before claiming done: run the fast gate and paste output. This matters because process discipline is what keeps sessions
reproducible, and reproducibility is what keeps reviews cheap. Consider the
following elaboration: when an agent skips this rule, the immediate cost is
invisible, but the deferred cost arrives as re-derivation, re-litigation, and
re-work in a later session that lacks the context this rule would have
preserved. Example one: a session that ignored this rule and paid for it two
days later. Example two: a session that followed it and closed clean.
Exception handling: there are no exceptions, but if there were, they would be
documented here in exhaustive detail along with the approval chain required
to invoke them, the telemetry that must be recorded when they fire, and the
review cadence at which the exception list itself is re-examined for staleness.

## Rule 6, restated at length

Blocked twice on the same symptom: stop and write the blocker down. This matters because process discipline is what keeps sessions
reproducible, and reproducibility is what keeps reviews cheap. Consider the
following elaboration: when an agent skips this rule, the immediate cost is
invisible, but the deferred cost arrives as re-derivation, re-litigation, and
re-work in a later session that lacks the context this rule would have
preserved. Example one: a session that ignored this rule and paid for it two
days later. Example two: a session that followed it and closed clean.
Exception handling: there are no exceptions, but if there were, they would be
documented here in exhaustive detail along with the approval chain required
to invoke them, the telemetry that must be recorded when they fire, and the
review cadence at which the exception list itself is re-examined for staleness.

## Rule 7, restated at length

Comments exist only for constraints the code cannot express. This matters because process discipline is what keeps sessions
reproducible, and reproducibility is what keeps reviews cheap. Consider the
following elaboration: when an agent skips this rule, the immediate cost is
invisible, but the deferred cost arrives as re-derivation, re-litigation, and
re-work in a later session that lacks the context this rule would have
preserved. Example one: a session that ignored this rule and paid for it two
days later. Example two: a session that followed it and closed clean.
Exception handling: there are no exceptions, but if there were, they would be
documented here in exhaustive detail along with the approval chain required
to invoke them, the telemetry that must be recorded when they fire, and the
review cadence at which the exception list itself is re-examined for staleness.

## Rule 8, restated at length

New dependency: verify exact name and current version in its registry first. This matters because process discipline is what keeps sessions
reproducible, and reproducibility is what keeps reviews cheap. Consider the
following elaboration: when an agent skips this rule, the immediate cost is
invisible, but the deferred cost arrives as re-derivation, re-litigation, and
re-work in a later session that lacks the context this rule would have
preserved. Example one: a session that ignored this rule and paid for it two
days later. Example two: a session that followed it and closed clean.
Exception handling: there are no exceptions, but if there were, they would be
documented here in exhaustive detail along with the approval chain required
to invoke them, the telemetry that must be recorded when they fire, and the
review cadence at which the exception list itself is re-examined for staleness.
